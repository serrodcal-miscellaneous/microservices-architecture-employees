image: gradle:4.0-jdk8

stages:
  - build
  - test

before_script:
  - apt-get update -y
  - gradle -version

build:
  stage: build
  script:
    - gradle clean build -x test

test:
  stage: test
  script:
    - gradle clean test